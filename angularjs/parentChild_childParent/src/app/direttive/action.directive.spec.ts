import { SomeActionDirective } from './action.directive';

describe('SomeActionDirective', () => {
  it('should create an instance', () => {
    const directive = new SomeActionDirective();
    expect(directive).toBeTruthy();
  });
});
